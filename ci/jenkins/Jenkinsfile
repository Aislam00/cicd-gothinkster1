pipeline {
    agent any
    
    environment {
        AWS_REGION = 'eu-west-2'
        ECR_REGISTRY = '475641479654.dkr.ecr.eu-west-2.amazonaws.com'
        IMAGE_NAME = 'realworld-api'
        ASG_NAME = 'realworld-dev-app-asg'
        NODE_ENV = 'test'
        PATH = "/var/lib/jenkins/.local/bin:$PATH"
    }
    
    stages {
        stage('Code Checkout') {
            steps {
                checkout scm
                sh 'find . -name "package.json" -type f | head -5'
            }
        }
        
        stage('Build & Install Dependencies') {
            steps {
                script {
                    if (fileExists('app/package.json')) {
                        dir('app') {
                            sh 'npm install'
                            sh 'npx nx build --prod'
                        }
                    }
                }
            }
        }
        
        stage('Security Scanning - Dependencies') {
            steps {
                script {
                    if (fileExists('app/package.json')) {
                        dir('app') {
                            sh '''
                                npm audit --audit-level high --json > npm-audit.json || true
                                npm audit --audit-level high || true
                                echo "✓ Dependency security scan completed"
                            '''
                        }
                    }
                }
            }
        }
        
        stage('Security Scanning - Infrastructure') {
            parallel {
                stage('Terraform Security - tfsec') {
                    steps {
                        script {
                            if (fileExists('terraform')) {
                                sh '''
                                    # Install tfsec without sudo
                                    curl -s https://raw.githubusercontent.com/aquasecurity/tfsec/master/scripts/install_linux.sh | bash -s -- -b $HOME/bin
                                    export PATH=$HOME/bin:$PATH
                                    $HOME/bin/tfsec terraform/ --format json --out tfsec-results.json || true
                                    $HOME/bin/tfsec terraform/ || true
                                    echo "✓ Terraform security scan completed"
                                '''
                            }
                        }
                    }
                }
                stage('Infrastructure Security - Checkov') {
                    steps {
                        script {
                            if (fileExists('terraform')) {
                                sh '''
                                    export PATH="/var/lib/jenkins/.local/bin:$PATH"
                                    checkov -d terraform/ --framework terraform --output json --output-file checkov-results.json || true
                                    checkov -d terraform/ --framework terraform --compact || true
                                    echo "✓ Infrastructure security scan completed"
                                '''
                            }
                        }
                    }
                }
            }
        }
        
        stage('Test Suite') {
            steps {
                script {
                    if (fileExists('app/package.json')) {
                        dir('app') {
                            sh '''
                                npx prisma generate
                                export DATABASE_URL="file:./test.db"
                                npx prisma db push --force-reset
                                npm test -- --watch=false --runInBand --forceExit --testPathIgnorePatterns="src/tests/services/auth.service.test.ts"
                            '''
                        }
                    }
                }
            }
        }
        
        stage('Docker Image Build') {
            steps {
                script {
                    def imageTag = "${BUILD_NUMBER}"
                    if (fileExists('app/Dockerfile')) {
                        dir('app') {
                            sh """
                                docker build --platform linux/amd64 -t ${ECR_REGISTRY}/${IMAGE_NAME}:${imageTag} .
                                docker tag ${ECR_REGISTRY}/${IMAGE_NAME}:${imageTag} ${ECR_REGISTRY}/${IMAGE_NAME}:latest
                            """
                        }
                    }
                    env.IMAGE_TAG = imageTag
                }
            }
        }
        
        stage('Container Security Scanning') {
            steps {
                sh '''
                    # Install Trivy to local directory
                    curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b $HOME/bin
                    export PATH=$HOME/bin:$PATH
                    $HOME/bin/trivy image --format json --output trivy-results.json ${ECR_REGISTRY}/${IMAGE_NAME}:${env.IMAGE_TAG} || true
                    $HOME/bin/trivy image --severity HIGH,CRITICAL ${ECR_REGISTRY}/${IMAGE_NAME}:${env.IMAGE_TAG} || true
                    echo "✓ Container security scan completed"
                '''
            }
        }
        
        stage('Container Registry Push') {
            steps {
                script {
                    withCredentials([aws(credentialsId: 'aws-credentials')]) {
                        sh """
                            aws ecr get-login-password --region ${AWS_REGION} | docker login --username AWS --password-stdin ${ECR_REGISTRY}
                            docker push ${ECR_REGISTRY}/${IMAGE_NAME}:${env.IMAGE_TAG}
                            docker push ${ECR_REGISTRY}/${IMAGE_NAME}:latest
                        """
                    }
                }
            }
        }
        
        stage('Production Deployment') {
            steps {
                script {
                    withCredentials([aws(credentialsId: 'aws-credentials')]) {
                        sh """
                            aws autoscaling start-instance-refresh \\
                                --auto-scaling-group-name ${ASG_NAME} \\
                                --preferences MinHealthyPercentage=50 \\
                                --region ${AWS_REGION}
                        """
                    }
                }
            }
        }
        
        stage('Health Check') {
            steps {
                sh """
                    echo "Waiting for new instances to come online..."
                    sleep 180
                    curl -f https://dev-api.iasolutions.co.uk/health || exit 1
                    echo "✓ Health check passed"
                """
            }
        }
    }
    
    post {
        always {
            sh 'docker system prune -f || true'
            archiveArtifacts artifacts: '*-results.json', allowEmptyArchive: true
        }
        success {
            echo "✓ Build #${BUILD_NUMBER} completed successfully with comprehensive security scanning"
        }
        failure {
            echo "✗ Build failed - Check security scan results in archived artifacts"
        }
    }
}
